"$schema" = "schemas/schema.json"

[default]
description = "The default permission for this plugin"
permissions = [
    "allow-client-streaming",
    "allow-duplex",
    "allow-server-streaming",
    "allow-unary",
]
